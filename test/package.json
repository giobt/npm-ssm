{
   "name":"test",
   "version":"1.0.0",
   "description":"Retrieve parameters from AWS Parameter Store and sets them as environment variables",
   "main":"test.js",
   "scripts":{
      "test":"echo \"Error: no test specified\" && exit 1"
   },
   "author":"nClouds DevOps",
   "license":"ISC",
   "dependencies":{
      "nclouds-paramstore":"latest"
   }
}
